// Include required libs like html2canvas for screenshot

// bypass CSP

// Screenshot

// Get DOM

// Get cookies
// Get HTTP headers from current document.location
var xhr = new XMLHttpRequest();
var headers = XMLHttpRequest.getAllResponseHeaders();

// if user logged (search for keywords like "logout", "user", "profile" etc, check for common session cookies, send request without credentials)
// a return of request without credentials should return a redirect to login so we can get the Login URL from this response

